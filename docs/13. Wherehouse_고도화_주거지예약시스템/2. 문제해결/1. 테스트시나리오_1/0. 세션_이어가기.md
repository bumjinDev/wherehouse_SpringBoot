# F004 시나리오 1 — 세션 이어가기 (2026-06-04 갱신)

> 2026-06-02 버전을 갱신. 06-04 세션에 **3차 B(비관적 락)·4차 C(낙관적 락) 측정까지 완료** — F004 4종
> (baseline·D·B·C) 측정·종합보고서가 **전부 끝났다.** B는 근거 문서(2세션 락 재현)도 작성. 도구 개선·폴더 재편 완료.
> **새 세션은 이 문서로 이어 가고, §5 결론은 다시 논의하지 말 것.** 남은 일은 **측정이 아니라 4채널 × 4종 최종 종합 문서**(미작성).
>
> ★ 기록 규칙: 아래는 **직접 확인된 것만 사실로** 적는다. 확인 안 된 것은 **"미확인"** 으로 명시한다. 추정을
> 사실처럼 쓰지 않는다.

---

## 0. 한 줄 상태
- **무엇을**: 슬롯 예약(F004) 동시성 제어 4유형 비교 — 단일 핫슬롯 · N=5 동시 · READ COMMITTED 고정.
- **완료(측정·보고)**: **1차 baseline · 2차 D · 3차 B · 4차 C — 4종 전부.** 각 4채널 측정 + xlsx + 종합보고서(.md). B는 근거 문서(2세션 락 재현)도 작성.
- **현재 코드**: createReservation = **④ C(낙관적 락 @Version)** 활성(`em.refresh` 제거·평문 findById + 엔티티 `@Version` + `VISIT_SLOT.VERSION` 컬럼, §4). 컴파일·측정 완료.
- **다음(측정 아님)**: **4채널 × 4종 최종 종합 문서** 작성(미작성). "무엇을 실제 채택할지"는 사용자 결정.
- **미마감**: 3차 B 보고서 §3.4 JMeter 응답시간/처리량 칸(slot-1237 런 화면 부재 = **미확인**, 채울지 보류).

---

## 1. 결정 목표 · 고정 틀
- **결정**: "슬롯 예약에 어떤 동시성 제어가 맞나" — baseline 대비 **D(조건부 UPDATE) / B(비관적 락) / C(낙관적 락 @Version)** 중 선택. 판단 축: 정합성(게이트) → 경합 시 거부되는 요청의 **차단 위치·비용** → 도메인(단일 핫슬롯).
- **고정 틀**: 같은 핫슬롯 1개 · N=5 동시(JMeter Synchronizing Timer) · RC 고정 · **unique 인덱스 부재**(붕괴/차단을 *제어 기법 자체*에 귀속).
- **4 관측 채널**:
  | 채널 | 출처 | 본다 |
  |---|---|---|
  | ⑴ 정합성 | DB 잔여 CONFIRMED | 슬롯당 확정 수 (1=OK / 2+=붕괴) |
  | ⑵ 단계 통과 분포 | 백엔드 로그 → report.py | 어느 단계까지 통과/거부 |
  | ⑶ SQL 타임라인 | P6Spy `took` → build_p6spy_xlsx.py | 거부 요청 비용·락 위치 |
  | ⑷ 외부 결과 | JMeter | 성공/거부·응답시간(클라이언트 관점) |

---

## 2. 4유형과 차단 위치

| 유형 | 슬롯 읽기 / 쓰기 | 차단 위치 | 슬롯당 CONFIRMED | 상태 |
|---|---|---|---|---|
| ① baseline | findById / 무조건 UPDATE | 없음(붕괴) | **5 (측정값)** | ✅ 완료 |
| ② D 조건부 UPDATE | findById / `UPDATE…WHERE status=AVAILABLE` | **쓰기**(`affected=0`) | **1 (측정값)** | ✅ 완료 |
| ③ B 비관적 락 | SELECT FOR UPDATE / 무조건 UPDATE | **읽기**(락 후 RESERVED 읽고 거부) | **1 (측정값)** | ✅ 완료 (측정 데이터는 1단계 `findByIdForUpdate` 코드 — §4) |
| ④ C 낙관적 락 | findById / 무조건 UPDATE + `@Version` | **커밋 flush**(version 조건 0행) | **1 (측정값)** | ✅ 완료 (slot 1170, §3) |

**★ 핵심(꼬임 방지)**: D(조건부 UPDATE)와 B(SELECT FOR UPDATE) **둘 다 비관적 행 X-락**이다. 다른 건 **락을 거는 위치(쓰기 vs 읽기)** 뿐이다. → "비관적 락을 UPDATE에 건다"는 곧 **D와 동일**하므로, B는 **SELECT FOR UPDATE로만** D와 구별된다. (자세한 근거 §5.)

---

## 3. 진행 현황 (확인된 사실)

- **1차 baseline** — slot **1218** (window 81). 한 슬롯에 **CONFIRMED 5건(붕괴)**, JMeter 5×201·오류 0%(조용한 붕괴). ORA-00001/백스톱 0(인덱스 부재).
- **2차 D (조건부 UPDATE)** — slot **1221**. **CONFIRMED 1건**(searcher03) + 4건 거부(쓰기 `affected=0`). JMeter 1×201 + 4×409·오류 80%. p6spy: 거부 UPDATE `took≈24ms`(락 대기) 후 롤백.
- **3차 B (비관적 락, SELECT FOR UPDATE)** — slot **1237**. **CONFIRMED 1건**(searcher05, resId 101) + 4건 거부. 차단=**읽기**: 후행 `SELECT … FOR UPDATE` `took` 계단 **2 / 73 / 97 / 112 / 128ms**, 후행은 확인조회(CP1)에서 **RESERVED** 를 읽고 거부(UPDATE·INSERT 미발행).
  - ※ 측정 데이터(slot 1237 xlsx)는 **1단계 `findByIdForUpdate`(SELECT FOR UPDATE)** 코드로 생성. 이후 사용자 지시로 락을 **2단계 `em.refresh(PESSIMISTIC_WRITE)`** 로 옮겨 컴파일(이 2단계 코드로는 미측정 — §4).
  - ※ 제공된 JMeter 화면은 **slot 1240의 다른 런**(전부 ~8.9s, 1×201+4×409)이라 slot-1237 보고서 §3.4 응답시간/처리량 칸은 **비움(미확인)**.
  - **근거 문서**: SQL Developer **2세션 락 직접 재현** 완료(`enq: TX - row lock contention` 직접 관측, 후행이 풀린 뒤 RESERVED 읽음). 보고서 §3.3·부록에 링크.
- **4차 C (낙관적 락, @Version)** — slot **1170**. **CONFIRMED 1건**(searcher03, resId 125) + 4건 거부. 락 없음 → 5명 모두 AVAILABLE(VERSION=0) 읽고 점유·예약 INSERT(헛 INSERT)까지 도달(확인조회 5 / 점유 5 / 등록 1). 거부 4건은 커밋 flush 의 `UPDATE … SET VERSION=1 WHERE VERSION=0` 이 **행 락 27ms 대기 후 0행** → `OptimisticLockException`. 차단=**커밋 flush(쓰기)**, 후행은 헛 INSERT 후 롤백(거부 비용 최대). JMeter **1×201 + 4×500**(미처리 예외 → Internal Server Error, **409 아님**)·오류 80%·평균 389ms.
  - ※ 1차 시도(slot 1202)는 **무효**: searcher03·04·05가 이전 회차 매물 693… 확정 보유 → 4단계 중복검증(`DuplicateReservationException`)에서 낙관락 전에 탈락(2명만 경합). → **예약 테이블 전체 삭제 후 slot 1170으로 재측정**(앱 로그로 4건 전부 `OptimisticLockException` 확정).
  - ★ 교훈: DB 정리는 **슬롯이 아니라 매물(693…) 단위** — 중복/시간겹침 검증이 매물 기준이라 슬롯만 바꿔선 오염이 안 풀린다.

도구 갱신: `build_p6spy_xlsx.py` 의 "스레드별 SQL (Oracle 실행형)" 시트 — 모든 시각 리터럴 → `TIMESTAMP '…'`, Hibernate 별칭(`vse1_0`…) 정리(단일 테이블은 별칭 제거 / JOIN은 `t1·t2` 치환).

산출물 위치는 §7(폴더 재편됨).

---

## 4. 현재 코드 상태 (확인됨)
- `VisitReservationWriteService.createReservation` = **④ C(낙관적 락 @Version)**:
  1단계 `slotRepository.findById(slotId)`(무락) → 2단계 `if (status != AVAILABLE)`(무락 검증, 5명 모두 통과) → 6단계 `setStatus(RESERVED)+save`(무조건 UPDATE).
  - **락 없음.** 차단은 커밋 flush 시 Hibernate 가 붙이는 `UPDATE … SET …, VERSION=VERSION+1 WHERE SLOT_ID=? AND VERSION=?` 의 0행 → `OptimisticLockException`.
  - 직전 B 형태의 `entityManager.refresh(slot, PESSIMISTIC_WRITE)`(2단계 락)는 **제거**됨. 안 쓰는 import/필드(EntityManager 등)도 제거.
  - 엔티티: `VisitSlotEntity.@Version private Long version;` + DB `VISIT_SLOT.VERSION NUMBER DEFAULT 0 NOT NULL` 적용됨.
- **로깅 = 구 `logSlotSnapshot`/`logReservationsOnSlot`** (마커 `CP1-AFTER-AVAILABLE-CHECK`/`CP2-AFTER-SLOT-UPDATE`/`CP3-AFTER-RESERVATION-INSERT`) → **report.py**. (C는 락이 없어 5명 다 CP2·INSERT 도달 → CP 분포로는 거부를 판정 못 함, **DB CONFIRMED=1 로 판정**.)
- repository: `findByIdForUpdate`·`reserveSlotIfAvailable` 둘 다 존재(현재 createReservation은 둘 다 미사용 — 평문 findById + @Version).
- **측정 종료 후 복원 시**: 엔티티 `@Version` 필드 + `VISIT_SLOT.VERSION` 컬럼 제거(부록 E).

---

## 5. 동시성 제어 핵심 결론 (★ 새 세션에서 다시 논의 금지)

1. **"비관적 락"은 두 가지로 실현된다.**
   - **조건부 UPDATE** (`UPDATE…WHERE status=AVAILABLE`): UPDATE 문 **자체가** 행 X-락을 잡는다. = **2차 D와 동일.** 차단=쓰기.
   - **SELECT FOR UPDATE**: 읽기에서 X-락. = **3차 B.** 차단=읽기. → **D와 구별되는 유일한 비관락 형태.**

2. **UPDATE 한 문장의 내부 절차(락 시점)**: ① 스냅샷 SCN 고정 → ② **WHERE로 후보 찾기 = 일관성 읽기(스냅샷, 무락·무블록)** → ③ **후보 행을 현재 모드로 집어 X-락 ← 여기서 락** → ④ SET → ⑤ commit까지 보유. 경합 시 후행은 ③에서 **대기**, 선행 commit 후 **현재값으로 재평가(write consistency / statement restart)** → `status≠AVAILABLE` → `affected=0`. (D의 거부가 이 원리.)

3. **baseline 붕괴 원인 = UPDATE가 아니라 그 앞의 별도 SELECT-검증(check-then-act)**. 5건이 각자 스냅샷에서 AVAILABLE을 읽어 검증을 다 통과했고 무조건 UPDATE가 그대로 덮어썼다. **D는 그 검증을 UPDATE의 WHERE로 합쳐** 해결.

4. **`@Lock(PESSIMISTIC_WRITE)`는 SELECT 전용**(SELECT에 FOR UPDATE 부여). `@Modifying` 벌크 UPDATE에는 **무효(no-op)**. 조건부 UPDATE는 어노테이션 없이도 이미 행 X-락을 잡는다.

5. **3차 B의 목표·관측 = D와의 직접 대비**(같은 "확정 1건"이라도 차단 지점이 다름):
   | 관측 | 2차 D (락=UPDATE) | 3차 B (락=SELECT) |
   |---|---|---|
   | 후행이 오래 기다리는 SQL | **UPDATE** 문 `took` 큼 | **SELECT** 문 `took` 큼 |
   | 후행 읽기 시점 슬롯상태(로그 CP1) | **AVAILABLE**(무락 스냅샷) | **RESERVED**(락 후 현재값) |
   | 후행 거부 방식 | `affected=0`(쓰기) | RESERVED 읽고 `if` 거부(읽기) |
   | 락 보유 구간 | UPDATE~commit(짧음) | SELECT~commit(김) |
   → 구 logSlotSnapshot 로깅을 쓰는 이유: **CP1 시점 슬롯상태(AVAILABLE↔RESERVED)** 가 D/B를 가르는 핵심인데 cp()는 그 스냅샷을 안 찍기 때문.
   → **이 대비는 3차 B 측정(§3)·2세션 재현(§6-B·근거 문서)으로 실측 확인됨**(후행 SELECT took 73~128ms, CP1=RESERVED).

6. **B vs C 결정**: **(a) B = SELECT FOR UPDATE 진행** ← 선택·완료. → 다음 **(c) C = @Version**.

7. **B에서 무엇을 실측할 수 있나 / 못 하나 (★ 락 블로킹 관찰의 한계)**
   - **잴 수 있다(JMeter 자동런)**: 후행 `SELECT … FOR UPDATE` 의 **`took`** 이 곧 **선행 트랜잭션이 commit될 때까지 기다린 시간**이다(P6Spy `took`은 락 대기 포함). → "선행 끝나기 전 후행 대기"는 수치로 실측됨. **(B에서 73~128ms로 확인됨.)**
   - **못 잡는다**: N=5는 ms 안에 끝나 `V$LOCK`으로 **"지금 누가 누구를 막는다"는 live 블로킹 스냅샷**은 못 잡는다(`took`·타임스탬프로 사후에만 봄).
   - **락 블로킹 자체를 live로 보려면 = 2세션 수동 demo(§6-B).** 이건 **앱을 안 거치는 DB-직접 관찰.** **2026-06-04 실행 완료**(근거 문서).

---

## 6. 측정 루틴 (회차 공통) — 4종 모두 이 순서로 완료

추가 측정(재현·반복) 시 동일 루틴:
1. **코드 변경** — 해당 회차 유형으로(`5. 테스트유형별_계측코드/` 참조).
2. **DB 정리** — searcher01~05 의 매물 `693…` **확정 예약 0** 으로(아래 검증 SQL). **★ 매물 단위로 봐야 한다**(중복/시간겹침 검증이 매물 기준 — 슬롯만 바꾸면 안 풀림). C 1차 무효의 원인이 이 누락이었다. (4차 때는 예약 테이블 전체 삭제로 해결: `delete from visit_notification;` → `delete from visit_reservation;` → commit. FK 때문에 notification 먼저.)
3. **새 슬롯** — 1218·1221·1237·1170(+무효 1202) 사용됨. 미래·AVAILABLE·window 81 슬롯 새로 선정 → CSV `3. 테스트파일_jmeter/users_sample_T1.csv` SLOT_ID 갱신.
4. **토큰 확인** — 11계정(부록 A) 재로그인으로 유효 토큰 재발급·CSV 반영(raw 토큰만, `Authorization=`·`; Path…` 제거).
5. **앱 재시작 → JMeter N=5 → 로그 백업 → report.py(앱계측 xlsx + §8 보고서) + build_p6spy_xlsx.py(타임라인).** ※ report.py=`--log NAME=PATH`/`--meta`/`--out`, build_p6spy=`--in`/`--out`/`--n`/`--env` (인자 다름, 혼동 주의).

검증 SQL(매물 잔여 확정 0 확인):
```sql
SELECT r.SEARCHER_USER_ID, COUNT(*) FROM VISIT_RESERVATION r
  JOIN VISIT_SLOT s ON r.SLOT_ID=s.SLOT_ID JOIN VISIT_WINDOW w ON s.WINDOW_ID=w.WINDOW_ID
 WHERE w.PROPERTY_ID='693c35731a5ebe74463679d39787c0c5' AND r.STATUS='CONFIRMED'
   AND r.SEARCHER_USER_ID IN ('searcher01','searcher02','searcher03','searcher04','searcher05')
 GROUP BY r.SEARCHER_USER_ID;
```

### 6-B. 락 블로킹 live 관찰 — 2세션 수동 demo (✅ B에서 실행 완료)
> **2026-06-04 실행 완료.** SQL Developer 2세션으로 **획득 → 차단(`enq: TX - row lock contention`) → 해제** 직접 관측. 후행이 풀린 뒤 선행이 커밋으로 남긴 값을 읽음을 두 경우로 확인(선행 ROLLBACK → 후행 AVAILABLE / 선행 RESERVED COMMIT → 후행 RESERVED = 거부 트리거). 905·906 슬롯 사용 후 원복 완료. 전체 기록 = 근거 문서 **`4. 보고서_테스트결과/3_1. 3차_B_비관적락_근거_2세션_락재현.md`**. 아래 스크립트는 재현용 보존(`V$` 조회는 권한 계정 필요, 단 "멈춤" 자체는 권한 없이도 보임).
```sql
-- 0) AVAILABLE 슬롯 하나 고르기
SELECT slot_id FROM VISIT_SLOT WHERE window_id=81 AND status='AVAILABLE' AND ROWNUM=1;

-- 1) 세션1: 행 X-락 보유 (commit/rollback 하지 말 것)
SELECT slot_id, status FROM VISIT_SLOT WHERE slot_id=<SLOT> FOR UPDATE;

-- 2) 세션2: 같은 행 → 멈춤(hang)
SELECT slot_id, status FROM VISIT_SLOT WHERE slot_id=<SLOT> FOR UPDATE;                 -- "선행 끝나기 전 후행 대기"
-- 또는: UPDATE VISIT_SLOT SET status='RESERVED' WHERE slot_id=<SLOT> AND status='AVAILABLE';  -- "락 중 UPDATE 막힘"

-- 3) (권한 계정) 블로킹 관계 live 확인
SELECT sid, username, event, blocking_session, seconds_in_wait
  FROM v$session WHERE event LIKE 'enq: TX%' OR blocking_session IS NOT NULL;

-- 4) 세션1: 풀기 → 세션2 즉시 완료  (RESERVED 커밋 후엔 후행이 RESERVED 읽음; 원복 잊지 말 것)
COMMIT;   -- 또는 ROLLBACK
```

---

## 7. 산출물 위치 (현재 디렉토리 구조 — 2026-06-04 재편)
```
1. 테스트시나리오_1/
├── 0. 세션_이어가기.md                       ← 본 문서
├── 1. Wherehouse_F004_시나리오1.md           (시나리오 정의 원본)
├── 1. 테스트결과_Application계측/            (옛 "1. 결과_Application계측")
│   ├── report.py · report.py_실행명령어.txt
│   ├── F004_측정결과_{baseline·D·B·C}.xlsx
│   └── 1_baseline_·2_D_·3_B_·4_C_낙관적락/   (각 wherehouse_*.log)
├── 2. 결과_p6spy로깅/
│   ├── build_p6spy_xlsx.py · 명령어_build_p6spy_xlsx.py.txt · 트랜잭션_관찰_쿼리.sql
│   └── 1_baseline_·2_D_·3_B_·4_C_낙관적락/   (각 log + …_타임라인.xlsx)
├── 3. 테스트파일_jmeter/   F004_시나리오1.jmx · users_sample_T1.csv · README 등  (옛 "jmeter")
├── 4. 보고서_테스트결과/    ← 보고서 모음(2026-06-04 신설)
│   ├── 1. 1차_baseline_무조건UPDATE_종합보고서.md
│   ├── 2. 2차_D_조건부UPDATE_종합보고서.md
│   ├── 3. 3차_B_비관적락_종합보고서.md
│   ├── 3_1. 3차_B_비관적락_근거_2세션_락재현.md
│   └── 4. 4차_C_낙관적락_종합보고서.md
├── 5. 테스트유형별_계측코드/   (참조용 4파일: 1_baseline·2_D·3_B·4_C .java)
├── 6. 테스트전_원본코드/       (VisitReservationWriteService·Repository 원본)
└── 7. 학습내용/               (동시성제어 종합정리·DB 격리수준 학습 .md)

코드: wherehouse/src/main/java/com/wherehouse/VisitReservation/
  ├── service/VisitReservationWriteService.java   (createReservation = C, 무락 + @Version)
  ├── entity/VisitSlotEntity.java                  (@Version version 필드 — 복원 시 제거)
  └── repository/VisitSlotRepository.java          (findByIdForUpdate·reserveSlotIfAvailable 존재·미사용)
```
- 보고서가 `4. 보고서_테스트결과/` 하위로 내려가, 보고서 안의 근거 파일 참조는 **`../1. 테스트결과_Application계측/…`·`../2. 결과_p6spy로깅/…`** 상대경로로 정정됨(같은 폴더 상호참조는 `3. …`·`3_1. …` 접두사).
- 실행 명령은 각 폴더의 `*_실행명령어.txt`. **py -3.13 필수**(openpyxl).
> ※ AI 비대화형 PowerShell은 한글 경로 인자가 깨진다 — AI가 직접 돌릴 땐 ASCII 임시 런처(경로를 스크립트 내 리터럴)로 우회.

---

## 8. 보고서 작성 규약 (확정)
- **종합보고서 = Markdown, 회차별 독립 1파일.** 5절 구조: ① 측정 목적 · ② 측정 환경 · ③ 채널별 결과(⑴DB·⑵단계분포·⑶발행 SQL·⑷JMeter) · ④ 메커니즘 해석(도식) · ⑤ 결론 + 근거 파일.
- **채널 ⑶(p6spy)**: 트랜잭션 begin/commit/rollback 로그·원본 바인딩 SQL 로그는 **제외**. **"Oracle 실행형으로 변형된 SQL"만** 싣고 **해석을 붙인다**(SQL 처음 보는 사람 기준). (build_p6spy_xlsx.py 가 시각 리터럴→TIMESTAMP, Hibernate 별칭 정리까지 해 둠 — §3.)
- **데이터 정합성**: 한 보고서에 **같은 런의 4채널만** 결합한다(다른 런 혼합 금지). 채울 수 없는 칸은 "미확인" 표기(예: 3차 §3.4).
- **용어 금지**: "CP/CP0~3" → 서술형(진입 / 슬롯 읽기 / 슬롯 점유 / 예약 등록). "승자/패자" → **"성공 요청 / 거부 요청"**(또는 선행/후행).
- **GitHub 공개 품질** 기준.

---

## 9. AI 작업 규칙 (★ 반드시 지킬 것)
- **추론을 사실처럼 단정 금지.** 내가 직접 연 파일·사용자가 준 화면에 있는 것만 사실로 쓴다. 없는 건 **"미확인"으로 비워두거나 묻는다.** 빈 곳을 그럴듯한 값으로 메우지 않는다.
- **돌려 말하지 말 것 — 직접.** 시킨 것·확인된 것만 한다. 임의 판단/적용 금지.
- 설계 결정(락·인덱스·격리·후보 선별)은 **사용자 몫.** AI는 옵션·근거만.
- 설명: **메커니즘 + 도식**, 비유·요약·임의 추가 금지, 단위 명시, 한국어.
- 시트/코드 구조는 사용자 명시 요청 외엔 손대지 말 것.

---

## 부록
- **A. 계정**: ownerTest1(`Owner1234!`) + searcher01~10(`Searcher01!`~`Searcher10!`). 인증은 **쿠키 `Authorization`** 로만. JMeter `Cookie: Authorization=${JWT_TOKEN}`.
- **B. 매물/윈도우**: property `693c35731a5ebe74463679d39787c0c5` / CHARTER / ownerTest1. **window 81**. slot 1218(baseline)·1221(D)·1237(B)·1170(C, +무효 1202) 사용됨. (2세션 demo는 905·906 별도 슬롯, 원복 완료.) ※ 4차 때 예약 테이블 전체 삭제 → 그 외 슬롯들은 예약 없이 STATUS=RESERVED 로 남아 있을 수 있음.
- **C. JMeter**: jmx 더블클릭 → ▶. `USERS_CSV`=users_sample_T1.csv. 끝나면 .jtl + `wherehouse/log/wherehouse.log` 결과 폴더로 백업.
- **D. 토큰**: 발급분 만료 이력 있음. **현재 유효 토큰 미확인** → C 측정 시 11계정 재로그인으로 재발급·확인(부록 A 인증 방식).
- **E. 측정 종료 후 복원**: P6Spy/트랜잭션 로깅(application.yml) off, CP 로깅 제거(선택), 동시성 제어는 결론 결과 적용, @Version(C 시) 필드+컬럼 정리, JWT 만료/스케줄러 주기 원복, unique 인덱스(`기타/SQL/03_constraints.sql`) 채택 시 복원. — 모두 **사용자 결정.**
```
